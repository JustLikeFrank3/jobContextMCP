# Pi-only Grafana extension:
#  - overrides the grafana-provisioning configmap (same name) to add the
#    Loki datasource alongside Prometheus
#  - adds a grafana-dashboard-pi configmap with the kiosk Cluster Health
#    dashboard; pi-deploy.sh patches the grafana deployment to mount it at
#    /var/lib/grafana/dashboards/pi (the base provider loads recursively).
# The wallboard rotation is OS-dependent: the workstation dual-boots, and
# the kiosk script picks a playlist by probing which GPU exporter is up
# (see pi-deploy.sh wallboard/kiosk-setup):
#    jcmcp-wallboard-linux:   kiosk-ollama + kiosk-cloud + kiosk-evals
#    jcmcp-wallboard-windows: kiosk-gaming + kiosk-cloud + kiosk-evals
# kiosk-gaming plots the same physical RTX 5060 Ti as kiosk-ollama — its
# gauges max() across both scrape jobs since only one OS is ever up.
# The other kiosk dashboards stay provisioned for manual viewing but are
# out of the rotation (pi-deploy.sh wallboard owns the playlist).
#
# Every stat panel reading cloud series (namespace jcmcp/jcmcp-qa, which
# reach this Prometheus only through the aks-federate job) sets
# "instant": true. Those panels default to a RANGE query reduced by
# lastNotNull, so when federation stops — the tunnel is Linux-only, so
# every boot into Windows kills it — they keep rendering the last value
# they ever saw, for the whole dashboard window. "Time since last suite
# run" froze at 19h for a day that way while the nightly evals were in
# fact running fine (2026-08-01). An instant query returns nothing once
# the series goes stale, so the panel says No data and the kiosk stops
# lying. Same failure the eval gauges hit in evals/ingest.restore_gauges.
#
# "instant" alone was not enough for the panels that ended in `or vector(0)`:
# vector(0) matches whether or not the feed is alive, so a dead feed read a
# reassuring green 0 ("no alerts", "no 5xx") instead of No data. Those now
# anchor the fallback to a series that only exists when the feed does —
#   X or (0 * max(<anchor>))
# with <anchor> = eval_last_run_timestamp_seconds for the eval panels and
# process_uptime_seconds for the cloud app panels. Feed alive and genuinely
# nothing to report still shows 0; feed gone shows No data. Verified against
# both Prometheis (AKS live, Pi stale) before landing.
#
# The up/local_llm_up/jobcontext_desktop_up panels previously kept `or vector(0)`
# on the theory that 0 -> DOWN is truthful when the exporter is gone. It is
# not, and the fallback only ever fires in the case where it lies:
#
#   up{job=X} is Prometheus-synthesised. A scrape that is attempted and fails
#   already yields 0 with no fallback at all. The series is ABSENT only when
#   Prometheus is not scraping that target — a monitoring gap, not a host
#   outage — and that is precisely when `or vector(0)` painted a red DOWN.
#
#   local_llm_up / jobcontext_desktop_up are relayed through the workstation
#   exporters, which always emit them as 0 or 1 while they are alive
#   (scripts/ollama-exporter.py: llm_lines emits 0 on a failed poll,
#   desktop_lines emits 0 when no sidecar port answers). So absent means the
#   exporter itself is gone — we have lost the observer, and claiming its
#   subjects are DOWN asserts something we cannot see.
#
# All four now carry no fallback and an explicit noValue, giving three
# distinguishable states: 1 UP, 0 DOWN (observed), absent NOT SCRAPED /
# NO EXPORTER. Unknown is not a verdict — same doctrine as no_record vs
# both_clean in the eval agreement buckets, and 503 vs 401 on an unverifiable
# JWKS in lib/auth.py.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-provisioning
  namespace: monitoring
data:
  datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        uid: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus.monitoring.svc:9090
        isDefault: true
      - name: Loki
        uid: loki
        type: loki
        access: proxy
        url: http://loki.monitoring.svc:3100
      # AKS Prometheus via the outbound tunnel held by aks-prom-tunnel.service
      # on the Pi host (see pi-deploy.sh aks-feed) — reached at the Pi's
      # static direct-link address (DHCP-immune).
      - name: Prometheus-AKS
        uid: prometheus-aks
        type: prometheus
        access: proxy
        url: http://192.168.101.2:9091
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: default
        orgId: 1
        folder: ""
        type: file
        options:
          path: /var/lib/grafana/dashboards
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-pi
  namespace: monitoring
data:
  kiosk-ollama.json: |
    {
      "uid": "kiosk-ollama", "title": "Local LLM — the Workstation", "tags": ["kiosk"],
      "timezone": "browser", "refresh": "30s", "schemaVersion": 39,
      "time": {"from": "now-3h", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 24, "h": 3}, "options": {"mode": "markdown", "content": "# 🧠 LOCAL LLM — whatever the workstation is serving · RTX 5060 Ti · either boot"}},
        {"type": "stat", "title": "LLM server", "gridPos": {"x":0,"y":3,"w":3,"h":6},
         "description": "local_llm_up from whichever workstation exporter is alive (:9105 Linux, :9106 Windows) — each probes the local OpenAI-compatible server (llama-server :8081, ollama :11434 fallback). Absent = both exporters gone, not a server verdict.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(local_llm_up{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"noValue": "NO EXPORTER", "mappings": [{"type": "value", "options": {"0": {"text": "DOWN", "color": "red"}, "1": {"text": "UP", "color": "green"}}}]}}},
        {"type": "stat", "title": "Model", "gridPos": {"x":3,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(model, server)(local_llm_info{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "{{model}} · {{server}}", "instant": true}],
         "fieldConfig": {"defaults": {"noValue": "none reported"}},
         "options": {"textMode": "name"}},
        {"type": "stat", "title": "Context window", "gridPos": {"x":7,"y":3,"w":3,"h":6},
         "description": "Absent when the running server does not report one — that is not a 0-token context.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(local_llm_context_length{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"noValue": "not reported"}}},
        {"type": "gauge", "title": "GPU temp", "gridPos": {"x":10,"y":3,"w":3,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(gpu_temperature_celsius{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "celsius", "min": 20, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":75},{"color":"red","value":90}]}}}},
        {"type": "gauge", "title": "GPU util", "gridPos": {"x":13,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(gpu_utilization_percent{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":80},{"color":"red","value":95}]}}}},
        {"type": "gauge", "title": "VRAM used", "gridPos": {"x":17,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100 * max(gpu_memory_used_bytes{job=~\"ollama-workstation|gpu-windows\"}) / max(gpu_memory_total_bytes{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":85},{"color":"red","value":95}]}}}},
        {"type": "stat", "title": "Desktop app", "gridPos": {"x":21,"y":3,"w":3,"h":6},
         "description": "The jobContext desktop sidecar relay is Linux-boot-only (:9105). Under Windows this reads NO EXPORTER — the observer is gone, not the app.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(jobcontext_desktop_up{job=\"ollama-workstation\"})"}],
         "fieldConfig": {"defaults": {"noValue": "NO EXPORTER", "mappings": [{"type": "value", "options": {"0": {"text": "DOWN", "color": "red"}, "1": {"text": "UP", "color": "green"}}}]}}},
        {"type": "timeseries", "title": "Token throughput (tok/s, 5m avg)", "gridPos": {"x":0,"y":9,"w":6,"h":9},
         "description": "llama.cpp's native counters, relayed by whichever exporter is up — they see every client (desktop, phone, Mac), not just the desktop app.",
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "sum(rate(llamacpp:tokens_predicted_total{job=~\"ollama-workstation|gpu-windows\"}[5m]))", "legendFormat": "generate"},
           {"expr": "sum(rate(llamacpp:prompt_tokens_total{job=~\"ollama-workstation|gpu-windows\"}[5m]))", "legendFormat": "prompt"}],
         "fieldConfig": {"defaults": {"min": 0}}},
        {"type": "timeseries", "title": "Tokens (1h)", "gridPos": {"x":6,"y":9,"w":6,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "sum(increase(llamacpp:tokens_predicted_total{job=~\"ollama-workstation|gpu-windows\"}[1h]))", "legendFormat": "generated"},
           {"expr": "sum(increase(llamacpp:prompt_tokens_total{job=~\"ollama-workstation|gpu-windows\"}[1h]))", "legendFormat": "prompt"},
           {"expr": "sum(increase(llamacpp:prompt_tokens_cached_total{job=~\"ollama-workstation|gpu-windows\"}[1h]))", "legendFormat": "prompt (cached)"}],
         "fieldConfig": {"defaults": {"min": 0}}},
        {"type": "timeseries", "title": "Requests", "gridPos": {"x":12,"y":9,"w":6,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "sum(llamacpp:requests_processing{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "processing"},
           {"expr": "sum(llamacpp:requests_deferred{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "queued"}],
         "fieldConfig": {"defaults": {"min": 0}}},
        {"type": "timeseries", "title": "Speculative accept % (MTP)", "gridPos": {"x":18,"y":9,"w":6,"h":9},
         "description": "Draft tokens accepted / drafted over 1h. Only the speed/balanced profiles load the MTP draft model; no data = no drafting in the window, which is not a 0% accept rate.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100 * sum(increase(llamacpp:spec_decode_num_accepted_tokens_total{job=~\"ollama-workstation|gpu-windows\"}[1h])) / sum(increase(llamacpp:spec_decode_num_draft_tokens_total{job=~\"ollama-workstation|gpu-windows\"}[1h]))", "legendFormat": "accept"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100}}},
        {"type": "timeseries", "title": "GPU utilization", "gridPos": {"x":0,"y":18,"w":8,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "max(gpu_utilization_percent{job=\"ollama-workstation\"})", "legendFormat": "Linux boot"},
           {"expr": "max(gpu_utilization_percent{job=\"gpu-windows\"})", "legendFormat": "Windows boot"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100}}},
        {"type": "timeseries", "title": "VRAM", "gridPos": {"x":8,"y":18,"w":8,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "max(gpu_memory_used_bytes{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "used"},
           {"expr": "max(gpu_memory_total_bytes{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "total"},
           {"expr": "sum(local_llm_model_size_bytes{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "model file size"}],
         "fieldConfig": {"defaults": {"unit": "bytes"}}},
        {"type": "timeseries", "title": "Desktop LLM calls (1h)", "gridPos": {"x":16,"y":18,"w":4,"h":9},
         "description": "From the desktop sidecar relay — Linux boot only.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(llm_calls_total{job=\"ollama-workstation\"}[1h])) by (model, outcome)", "legendFormat": "{{model}}/{{outcome}}"}]},
        {"type": "timeseries", "title": "Desktop call latency", "gridPos": {"x":20,"y":18,"w":4,"h":9},
         "description": "From the desktop sidecar relay — Linux boot only.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(model)(rate(llm_call_seconds_sum{job=\"ollama-workstation\"}[15m])) / sum by(model)(rate(llm_call_seconds_count{job=\"ollama-workstation\"}[15m]))", "legendFormat": "{{model}}"}],
         "fieldConfig": {"defaults": {"unit": "s"}}}
      ]
    }
  kiosk-gaming.json: |
    {
      "uid": "kiosk-gaming", "title": "Flight Sim Rig — GPU (Windows)", "tags": ["kiosk"],
      "timezone": "browser", "refresh": "30s", "schemaVersion": 39,
      "time": {"from": "now-3h", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 24, "h": 3}, "options": {"mode": "markdown", "content": "# 🛩️ FLIGHT SIM RIG — RTX 5060 Ti · Windows"}},
        {"type": "stat", "title": "Workstation boot", "gridPos": {"x":0,"y":3,"w":8,"h":6},
         "description": "The workstation dual-boots; exactly one of the two exporters can be up. 1=Linux (:9105 over the direct link), 2=Windows (:9106 over the LAN lease), 0=neither reachable, 3=impossible and means a stale target. No data = a job is missing from the scrape config, so the boot state is unknowable.",
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(up{job=\"ollama-workstation\"}) + 2 * sum(up{job=\"gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"noValue": "NOT SCRAPED", "mappings": [{"type": "value", "options": {"0": {"text": "OFF", "color": "text"}, "1": {"text": "LINUX", "color": "green"}, "2": {"text": "WINDOWS", "color": "blue"}, "3": {"text": "BOTH?! stale target", "color": "red"}}}]}}},
        {"type": "gauge", "title": "GPU util", "gridPos": {"x":8,"y":3,"w":6,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(gpu_utilization_percent{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":80},{"color":"red","value":95}]}}}},
        {"type": "gauge", "title": "VRAM used", "gridPos": {"x":14,"y":3,"w":5,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100 * max(gpu_memory_used_bytes{job=~\"ollama-workstation|gpu-windows\"}) / max(gpu_memory_total_bytes{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":85},{"color":"red","value":95}]}}}},
        {"type": "gauge", "title": "GPU temp", "gridPos": {"x":19,"y":3,"w":5,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(gpu_temperature_celsius{job=~\"ollama-workstation|gpu-windows\"})"}],
         "fieldConfig": {"defaults": {"unit": "celsius", "min": 20, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":75},{"color":"red","value":90}]}}}},
        {"type": "timeseries", "title": "GPU utilization", "gridPos": {"x":0,"y":9,"w":12,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "max(gpu_utilization_percent{job=\"ollama-workstation\"})", "legendFormat": "workstation"},
           {"expr": "max(gpu_utilization_percent{job=\"gpu-windows\"})", "legendFormat": "flight-sim rig"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100}}},
        {"type": "timeseries", "title": "GPU temp", "gridPos": {"x":12,"y":9,"w":12,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "max(gpu_temperature_celsius{job=\"ollama-workstation\"})", "legendFormat": "workstation"},
           {"expr": "max(gpu_temperature_celsius{job=\"gpu-windows\"})", "legendFormat": "flight-sim rig"}],
         "fieldConfig": {"defaults": {"unit": "celsius"}}},
        {"type": "timeseries", "title": "VRAM", "gridPos": {"x":0,"y":18,"w":24,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "max(gpu_memory_used_bytes{job=\"ollama-workstation\"})", "legendFormat": "workstation used"},
           {"expr": "max(gpu_memory_used_bytes{job=\"gpu-windows\"})", "legendFormat": "flight-sim rig used"},
           {"expr": "max(gpu_memory_total_bytes{job=~\"ollama-workstation|gpu-windows\"})", "legendFormat": "total"}],
         "fieldConfig": {"defaults": {"unit": "bytes"}}}
      ]
    }
  kiosk-app.json: |
    {
      "uid": "kiosk-app", "title": "Application Health — jobContext (Pi)", "tags": ["kiosk"],
      "timezone": "browser", "refresh": "30s", "schemaVersion": 39,
      "time": {"from": "now-3h", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 24, "h": 3}, "options": {"mode": "markdown", "content": "# \ud83d\udcf1 APPLICATION \u2014 jobContext on the Pi"}},
        {"type": "timeseries", "title": "Request rate by route", "gridPos": {"x":0,"y":3,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(route)(label_replace(rate(http_requests_total{namespace=\"jcmcp-pi\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\"))", "legendFormat": "{{route}}"}],
         "fieldConfig": {"defaults": {"unit": "reqps"}}},
        {"type": "timeseries", "title": "Avg latency by route", "gridPos": {"x":8,"y":3,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(route)(label_replace(rate(http_request_seconds_sum{namespace=\"jcmcp-pi\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\")) / sum by(route)(label_replace(rate(http_request_seconds_count{namespace=\"jcmcp-pi\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\"))", "legendFormat": "{{route}}"}],
         "fieldConfig": {"defaults": {"unit": "s"}}},
        {"type": "timeseries", "title": "5xx error rate", "gridPos": {"x":16,"y":3,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(rate(http_requests_total{namespace=\"jcmcp-pi\",status=~\"5..\"}[5m])) or vector(0)"}],
         "fieldConfig": {"defaults": {"thresholds": {"steps": [{"color":"green","value":null},{"color":"red","value":0.01}]}}}},
        {"type": "timeseries", "title": "Work items (1h) by kind/status", "gridPos": {"x":0,"y":11,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(work_items_total{namespace=\"jcmcp-pi\"}[1h])) by (kind, status)", "legendFormat": "{{kind}}/{{status}}"}]},
        {"type": "timeseries", "title": "Avg work-item duration", "gridPos": {"x":8,"y":11,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "rate(work_item_seconds_sum{namespace=\"jcmcp-pi\"}[15m]) / rate(work_item_seconds_count{namespace=\"jcmcp-pi\"}[15m])", "legendFormat": "{{kind}}"}],
         "fieldConfig": {"defaults": {"unit": "s"}}},
        {"type": "timeseries", "title": "LLM calls by model/outcome", "gridPos": {"x":16,"y":11,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(llm_calls_total{namespace=\"jcmcp-pi\"}[1h])) by (model, outcome)", "legendFormat": "{{model}}/{{outcome}}"}]},
        {"type": "timeseries", "title": "LLM tokens/hour by direction", "gridPos": {"x":0,"y":19,"w":16,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(llm_tokens_total{namespace=\"jcmcp-pi\"}[1h])) by (model, direction)", "legendFormat": "{{model}} {{direction}}"}]},
        {"type": "stat", "title": "App uptime", "gridPos": {"x":16,"y":19,"w":8,"h":8},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(process_uptime_seconds{namespace=\"jcmcp-pi\"})"}],
         "fieldConfig": {"defaults": {"unit": "s"}}}
      ]
    }
  kiosk-cluster.json: |
    {
      "uid": "kiosk-cluster", "title": "Cluster Health — Node1", "tags": ["kiosk"],
      "timezone": "browser", "refresh": "30s", "schemaVersion": 39,
      "time": {"from": "now-3h", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 24, "h": 3}, "options": {"mode": "markdown", "content": "# \ud83d\udda5\ufe0f CLUSTER \u2014 Node1 k3s: pods & hardware"}},
        {"type": "stat", "title": "Pods running", "gridPos": {"x":0,"y":3,"w":3,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(kube_pod_status_phase{phase=\"Running\"})"}]},
        {"type": "stat", "title": "Pods down", "gridPos": {"x":3,"y":3,"w":3,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(kube_pod_status_phase{phase=~\"Pending|Failed|Unknown\"}) or vector(0)"}],
         "fieldConfig": {"defaults": {"thresholds": {"steps": [{"color":"green","value":null},{"color":"red","value":1}]}}}},
        {"type": "stat", "title": "Restarts (24h)", "gridPos": {"x":6,"y":3,"w":3,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(kube_pod_container_status_restarts_total[24h])) or vector(0)"}],
         "fieldConfig": {"defaults": {"thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":1},{"color":"red","value":5}]}}}},
        {"type": "gauge", "title": "CPU temp", "gridPos": {"x":9,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "avg(node_thermal_zone_temp)"}],
         "fieldConfig": {"defaults": {"unit": "celsius", "min": 20, "max": 90,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":65},{"color":"red","value":80}]}}}},
        {"type": "gauge", "title": "CPU", "gridPos": {"x":13,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100*(1-avg(rate(node_cpu_seconds_total{mode=\"idle\"}[5m])))"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":70},{"color":"red","value":90}]}}}},
        {"type": "gauge", "title": "RAM", "gridPos": {"x":17,"y":3,"w":4,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100*(1-node_memory_MemAvailable_bytes/node_memory_MemTotal_bytes)"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":75},{"color":"red","value":90}]}}}},
        {"type": "bargauge", "title": "Disk", "gridPos": {"x":21,"y":3,"w":3,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100*(1-node_filesystem_avail_bytes{mountpoint=~\"/|/mnt/backup\"}/node_filesystem_size_bytes{mountpoint=~\"/|/mnt/backup\"})", "legendFormat": "{{mountpoint}}"}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100,
           "thresholds": {"steps": [{"color":"green","value":null},{"color":"orange","value":75},{"color":"red","value":90}]}}}},
        {"type": "timeseries", "title": "CPU by pod", "gridPos": {"x":0,"y":9,"w":12,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(pod)(rate(container_cpu_usage_seconds_total{container!=\"\",pod!=\"\"}[5m]))", "legendFormat": "{{pod}}"}],
         "fieldConfig": {"defaults": {"unit": "percentunit"}}},
        {"type": "timeseries", "title": "Memory by pod", "gridPos": {"x":12,"y":9,"w":12,"h":6},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(pod)(container_memory_working_set_bytes{container!=\"\",pod!=\"\"})", "legendFormat": "{{pod}}"}],
         "fieldConfig": {"defaults": {"unit": "bytes"}}},
        {"type": "timeseries", "title": "Network eth0/wlan0", "gridPos": {"x":0,"y":15,"w":12,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "rate(node_network_receive_bytes_total{device=~\"eth0|wlan0\"}[5m])", "legendFormat": "rx {{device}}"},
           {"expr": "rate(node_network_transmit_bytes_total{device=~\"eth0|wlan0\"}[5m])", "legendFormat": "tx {{device}}"}],
         "fieldConfig": {"defaults": {"unit": "Bps"}}},
        {"type": "timeseries", "title": "Disk I/O (SD + USB)", "gridPos": {"x":12,"y":15,"w":12,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [
           {"expr": "rate(node_disk_written_bytes_total{device=~\"mmcblk0|sda\"}[5m])", "legendFormat": "write {{device}}"},
           {"expr": "rate(node_disk_read_bytes_total{device=~\"mmcblk0|sda\"}[5m])", "legendFormat": "read {{device}}"}],
         "fieldConfig": {"defaults": {"unit": "Bps"}}},
        {"type": "logs", "title": "Errors — all namespaces", "gridPos": {"x":0,"y":20,"w":24,"h":7},
         "datasource": {"uid":"loki"},
         "options": {"showTime": true, "sortOrder": "Descending"},
         "targets": [{"expr": "{namespace=~\".+\"} |~ \"(?i)(error|traceback|exception|failed)\" != \"level=info\" != \"notifying scheduler\""}]}
      ]
    }
  kiosk-cloud.json: |
    {
      "uid": "kiosk-cloud", "title": "Cloud Health — jobcontext.ai (AKS)", "tags": ["kiosk"],
      "timezone": "browser", "refresh": "30s", "schemaVersion": 39,
      "time": {"from": "now-3h", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 24, "h": 3}, "options": {"mode": "markdown", "content": "# \u2601\ufe0f PRODUCTION \u2014 jobcontext.ai on AKS"}},
        {"type": "stat", "title": "prod uptime", "gridPos": {"x":0,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(process_uptime_seconds{namespace=\"jcmcp\"})", "instant": true}],
         "fieldConfig": {"defaults": {"unit": "s"}}},
        {"type": "stat", "title": "qa uptime", "gridPos": {"x":4,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "max(process_uptime_seconds{namespace=\"jcmcp-qa\"})", "instant": true}],
         "fieldConfig": {"defaults": {"unit": "s"}}},
        {"type": "stat", "title": "5xx rate (prod)", "gridPos": {"x":8,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(rate(http_requests_total{namespace=\"jcmcp\",status=~\"5..\"}[5m])) or (0 * max(process_uptime_seconds{namespace=\"jcmcp\"}))", "instant": true}],
         "fieldConfig": {"defaults": {"thresholds": {"steps": [{"color":"green","value":null},{"color":"red","value":0.01}]}}}},
        {"type": "stat", "title": "Work items (1h, prod)", "gridPos": {"x":12,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(work_items_total{namespace=\"jcmcp\"}[1h])) or (0 * max(process_uptime_seconds{namespace=\"jcmcp\"}))", "instant": true}]},
        {"type": "stat", "title": "LLM calls (1h, prod)", "gridPos": {"x":16,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(llm_calls_total{namespace=\"jcmcp\"}[1h])) or (0 * max(process_uptime_seconds{namespace=\"jcmcp\"}))", "instant": true}]},
        {"type": "stat", "title": "Gate pass rate (24h, prod)", "gridPos": {"x":20,"y":3,"w":4,"h":5},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "100 * sum(increase(provenance_checks_total{namespace=\"jcmcp\",verdict=\"passed\"}[24h])) / sum(increase(provenance_checks_total{namespace=\"jcmcp\"}[24h]))", "instant": true}],
         "fieldConfig": {"defaults": {"unit": "percent", "min": 0, "max": 100, "noValue": "no runs",
           "thresholds": {"steps": [{"color":"text","value":null},{"color":"red","value":1},{"color":"orange","value":80},{"color":"green","value":99}]}}}},
        {"type": "timeseries", "title": "Request rate by route (prod)", "gridPos": {"x":0,"y":8,"w":12,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(route)(label_replace(rate(http_requests_total{namespace=\"jcmcp\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\"))", "legendFormat": "{{route}}"}],
         "fieldConfig": {"defaults": {"unit": "reqps"}}},
        {"type": "timeseries", "title": "Avg latency by route (prod)", "gridPos": {"x":12,"y":8,"w":12,"h":9},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(route)(label_replace(rate(http_request_seconds_sum{namespace=\"jcmcp\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\")) / sum by(route)(label_replace(rate(http_request_seconds_count{namespace=\"jcmcp\"}[5m]), \"route\", \"static\", \"route\", \"/favicon.*\"))", "legendFormat": "{{route}}"}],
         "fieldConfig": {"defaults": {"unit": "s"}}},
        {"type": "timeseries", "title": "Work items by kind/status", "gridPos": {"x":0,"y":17,"w":8,"h":10},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(work_items_total[1h])) by (kind, status)", "legendFormat": "{{kind}}/{{status}}"}]},
        {"type": "timeseries", "title": "LLM tokens/hour by direction", "gridPos": {"x":8,"y":17,"w":8,"h":10},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum(increase(llm_tokens_total[1h])) by (model, direction)", "legendFormat": "{{model}} {{direction}}"}]},
        {"type": "timeseries", "title": "Provenance gate verdicts (prod)", "gridPos": {"x":16,"y":17,"w":8,"h":10},
         "datasource": {"uid":"prometheus"},
         "targets": [{"expr": "sum by(verdict)(increase(provenance_checks_total{namespace=~\"jcmcp|jcmcp-qa\"}[1h]))", "legendFormat": "{{verdict}}"}]}
      ]
    }
  kiosk-provenance.json: |
    {
      "uid": "kiosk-provenance",
      "title": "Provenance — Generation Truth Gate",
      "tags": [
        "kiosk"
      ],
      "timezone": "browser",
      "refresh": "30s",
      "schemaVersion": 39,
      "time": {
        "from": "now-24h",
        "to": "now"
      },
      "panels": [
        {
          "type": "text",
          "title": "",
          "transparent": true,
          "gridPos": {
            "x": 0,
            "y": 0,
            "w": 24,
            "h": 3
          },
          "options": {
            "mode": "markdown",
            "content": "# 🔎 PROVENANCE — generation truth gate · Node1 hardware"
          }
        },
        {
          "type": "stat",
          "title": "Checks (total)",
          "gridPos": {
            "x": 0,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "sum(provenance_runs_total) or vector(0)"
            }
          ]
        },
        {
          "type": "stat",
          "title": "Pass rate",
          "gridPos": {
            "x": 4,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "100 * sum(provenance_runs_total{verdict=\"passed\"}) / clamp_min(sum(provenance_runs_total), 1)"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "percent",
              "min": 0,
              "max": 100,
              "thresholds": {
                "steps": [
                  {
                    "color": "red",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 80
                  },
                  {
                    "color": "green",
                    "value": 99
                  }
                ]
              }
            }
          }
        },
        {
          "type": "stat",
          "title": "Violations caught",
          "gridPos": {
            "x": 8,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "sum(provenance_violations_recorded_total) or vector(0)"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "thresholds": {
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 1
                  }
                ]
              }
            }
          }
        },
        {
          "type": "gauge",
          "title": "CPU temp",
          "gridPos": {
            "x": 12,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "avg(node_thermal_zone_temp)"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "celsius",
              "min": 20,
              "max": 90,
              "thresholds": {
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 65
                  },
                  {
                    "color": "red",
                    "value": 80
                  }
                ]
              }
            }
          }
        },
        {
          "type": "gauge",
          "title": "CPU",
          "gridPos": {
            "x": 16,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "100*(1-avg(rate(node_cpu_seconds_total{mode=\"idle\"}[5m])))"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "percent",
              "min": 0,
              "max": 100,
              "thresholds": {
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 70
                  },
                  {
                    "color": "red",
                    "value": 90
                  }
                ]
              }
            }
          }
        },
        {
          "type": "gauge",
          "title": "RAM",
          "gridPos": {
            "x": 20,
            "y": 3,
            "w": 4,
            "h": 6
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "100*(1-node_memory_MemAvailable_bytes/node_memory_MemTotal_bytes)"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "percent",
              "min": 0,
              "max": 100,
              "thresholds": {
                "steps": [
                  {
                    "color": "green",
                    "value": null
                  },
                  {
                    "color": "orange",
                    "value": 75
                  },
                  {
                    "color": "red",
                    "value": 90
                  }
                ]
              }
            }
          }
        },
        {
          "type": "timeseries",
          "title": "Gate checks by verdict",
          "gridPos": {
            "x": 0,
            "y": 9,
            "w": 12,
            "h": 9
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "sum by(verdict)(increase(provenance_checks_total[1h]))",
              "legendFormat": "{{verdict}}"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Violations over time",
          "gridPos": {
            "x": 12,
            "y": 9,
            "w": 12,
            "h": 9
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "sum(increase(provenance_violations_total[1h])) or vector(0)",
              "legendFormat": "violations/h"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Network eth0/wlan0",
          "gridPos": {
            "x": 0,
            "y": 18,
            "w": 12,
            "h": 9
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "rate(node_network_receive_bytes_total{device=~\"eth0|wlan0\"}[5m])",
              "legendFormat": "rx {{device}}"
            },
            {
              "expr": "rate(node_network_transmit_bytes_total{device=~\"eth0|wlan0\"}[5m])",
              "legendFormat": "tx {{device}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "Bps"
            }
          }
        },
        {
          "type": "timeseries",
          "title": "Disk I/O (SD + USB)",
          "gridPos": {
            "x": 12,
            "y": 18,
            "w": 12,
            "h": 9
          },
          "datasource": {
            "uid": "prometheus"
          },
          "targets": [
            {
              "expr": "rate(node_disk_written_bytes_total{device=~\"mmcblk0|sda\"}[5m])",
              "legendFormat": "write {{device}}"
            },
            {
              "expr": "rate(node_disk_read_bytes_total{device=~\"mmcblk0|sda\"}[5m])",
              "legendFormat": "read {{device}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "Bps"
            }
          }
        }
      ]
    }
  kiosk-evals.json: |
    {
      "uid": "kiosk-evals", "title": "Evals — resume quality (LLM-as-judge)", "tags": ["kiosk"],
      "links": [
        {"type": "link", "title": "📋 Run detail — alerts, flips, flagged claims", "icon": "external link",
         "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}
      ],
      "timezone": "browser", "refresh": "1m", "schemaVersion": 39,
      "time": {"from": "now-7d", "to": "now"},
      "panels": [
        {"type": "text", "title": "", "transparent": true, "gridPos": {"x": 0, "y": 0, "w": 15, "h": 3},
         "options": {"mode": "markdown", "content": "# 🧪 EVALS — golden dataset · LLM-as-judge · N-run variance\n\n⚠️ Accuracy is the weak dimension. Calibrated accuracy MAE vs blind human labels, all at max_tokens=8000 — claude-sonnet-5 **1.48 / 1.56** over two N=5 passes but calibrated on 4 of 5 entries (GD-03: accuracy 5.0 against a human 1, zero flags, both passes) · gpt-4.1-mini **3.2**, a constant 5.0 on every entry so it ranks nothing · llama3.1:8b **3.2**. A judge not listed has no calibration table (docs/evals.md)."}},
        {"type": "stat", "title": "", "gridPos": {"x": 15, "y": 0, "w": 4, "h": 3},
         "description": "Solid tile as a touch target: kiosk mode hides dashboard chrome and markdown can't be styled past the sanitizer, so the button IS a panel. vector(1) is a constant — the tile exists to be tapped, not to measure.",
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "vector(1)", "instant": true}],
         "options": {"textMode": "name", "colorMode": "background", "graphMode": "none", "justifyMode": "center"},
         "fieldConfig": {"defaults": {"displayName": "📋 RUN DETAIL",
           "color": {"mode": "fixed", "fixedColor": "#3274D9"},
           "links": [{"title": "Run detail & work stats", "url": "http://192.168.71.51/wallboard/evals", "targetBlank": false}]}}},
        {"type": "stat", "title": "Judge (last scored run)", "gridPos": {"x": 19, "y": 0, "w": 5, "h": 3},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "eval_judge_info{namespace=\"jcmcp\"}", "instant": true, "legendFormat": "{{judge}}"}],
         "options": {"textMode": "name", "colorMode": "none"},
         "fieldConfig": {"defaults": {"noValue": "NO SCORED RUN"}}},
        {"type": "stat", "title": "Mean judge score (worst entry)", "gridPos": {"x": 0, "y": 3, "w": 5, "h": 6},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "min(eval_mean_score{namespace=\"jcmcp\"})", "instant": true}],
         "fieldConfig": {"defaults": {"min": 1, "max": 5, "decimals": 2,
          "thresholds": {"steps": [{"color": "red", "value": null},
           {"color": "green", "value": 4}]}}}},
        {"type": "stat", "title": "Hallucinations", "gridPos": {"x": 5, "y": 3, "w": 5, "h": 6},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "max(eval_hallucination_rate_pct{namespace=\"jcmcp\"})", "instant": true}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "unit": "percent", "decimals": 0,
          "thresholds": {"steps": [{"color": "green", "value": null}, {"color": "red", "value": 0.001}]}}}},
        {"type": "stat", "title": "Verdict flips", "gridPos": {"x": 10, "y": 3, "w": 5, "h": 6},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "max(eval_verdict_flip_rate_pct{namespace=\"jcmcp\"})", "instant": true}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "unit": "percent", "decimals": 0,
          "thresholds": {"steps": [{"color": "green", "value": null}, {"color": "orange", "value": 20}]}}}},
        {"type": "stat", "title": "Layer 1 smoke", "gridPos": {"x": 15, "y": 3, "w": 5, "h": 6},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "max(eval_layer1_smoke_pass_rate_pct{namespace=\"jcmcp\"})", "instant": true}],
         "fieldConfig": {"defaults": {"unit": "percent", "decimals": 0,
          "thresholds": {"steps": [{"color": "red", "value": null},
           {"color": "orange", "value": 95}, {"color": "green", "value": 100}]}}}},
        {"type": "stat", "title": "Alerts", "gridPos": {"x": 20, "y": 3, "w": 4, "h": 6},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "sum(max by(gd_id)(eval_alert_count{namespace=\"jcmcp\"})) or (0 * max(eval_last_run_timestamp_seconds{namespace=\"jcmcp\"}))", "instant": true}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "decimals": 0,
          "thresholds": {"steps": [{"color": "green", "value": null}, {"color": "red", "value": 1}]}}}},
        {"type": "timeseries", "title": "Mean judge score by golden entry", "gridPos": {"x": 0, "y": 9, "w": 12, "h": 9},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "max by(gd_id)(eval_mean_score{namespace=\"jcmcp\"})", "legendFormat": "{{gd_id}}"}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "min": 1, "max": 5,
          "custom": {"thresholdsStyle": {"mode": "line"}},
          "thresholds": {"steps": [{"color": "transparent", "value": null}, {"color": "red", "value": 4}]}}}},
        {"type": "timeseries", "title": "CoV % (instability) by golden entry", "gridPos": {"x": 12, "y": 9, "w": 12, "h": 9},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "max by(gd_id)(eval_cov_pct{namespace=\"jcmcp\"})", "legendFormat": "{{gd_id}}"}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "unit": "percent", "min": 0,
          "custom": {"thresholdsStyle": {"mode": "line"}},
          "thresholds": {"steps": [{"color": "transparent", "value": null}, {"color": "orange", "value": 20}]}}}},
        {"type": "bargauge", "title": "Per-dimension mean (across entries)", "gridPos": {"x": 0, "y": 18, "w": 12, "h": 8},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "avg by(dimension)(eval_dimension_score{namespace=\"jcmcp\"})", "legendFormat": "{{dimension}}"}],
         "options": {"displayMode": "gradient", "orientation": "horizontal"},
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "min": 1, "max": 5, "decimals": 2,
          "thresholds": {"steps": [{"color": "red", "value": null},
           {"color": "orange", "value": 3}, {"color": "green", "value": 4}]}}}},
        {"type": "stat", "title": "Time since last suite run", "gridPos": {"x": 12, "y": 18, "w": 6, "h": 8},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "time() - max(eval_last_run_timestamp_seconds{namespace=\"jcmcp\",kind=\"suite\"})", "instant": true}],
         "fieldConfig": {"defaults": {"unit": "s", "decimals": 0,
          "thresholds": {"steps": [{"color": "green", "value": null}, {"color": "orange", "value": 604800}]}}}},
        {"type": "timeseries", "title": "Eval pushes by kind (1d)", "gridPos": {"x": 18, "y": 18, "w": 6, "h": 8},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "sum by(kind)(increase(eval_pushes_total{namespace=\"jcmcp\"}[1d]))", "legendFormat": "{{kind}}"}]},
        {"type": "stat", "title": "Runs without provenance record", "gridPos": {"x": 0, "y": 26, "w": 6, "h": 7},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "sum(max by(gd_id)(eval_provenance_agreement_count{namespace=\"jcmcp\",kind=\"no_record\"}))", "instant": true}],
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "decimals": 0,
          "thresholds": {"steps": [{"color": "green", "value": null}, {"color": "red", "value": 1}]}}}},
        {"type": "bargauge", "title": "Provenance agreement by bucket (raw counts)", "gridPos": {"x": 6, "y": 26, "w": 18, "h": 7},
         "datasource": {"uid": "prometheus"},
         "targets": [{"expr": "sum by(kind)(max by(kind,gd_id)(eval_provenance_agreement_count{namespace=\"jcmcp\"}))", "legendFormat": "{{kind}}"}],
         "options": {"displayMode": "gradient", "orientation": "horizontal"},
         "fieldConfig": {"defaults": {"links": [{"title": "Run detail \u2014 alerts, flips, flagged claims (JSON; needs dashboard login)", "url": "https://jobcontext.ai/api/evals/results", "targetBlank": true}], "min": 0, "decimals": 0}}}
      ]
    }
