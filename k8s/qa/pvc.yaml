apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jcmcp-qa-data
  namespace: jcmcp-qa
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi  # Azure Standard SSD — cost-optimised for QA
  resources:
    requests:
      storage: 5Gi
